{
    "name": "mern-developer-blog",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && npm run dev -- --host 0.0.0.0",
        "server": "cd server && npm run dev",
        "start": "concurrently \"npm run server\" \"npm run client\"",
        "build": "cd client && npm run build"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    },
    "dependencies": {
        "nodemailer": "^10.0.10"
    }
}
