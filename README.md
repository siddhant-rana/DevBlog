# MERN Developer Blog

Project structure placeholder.

## Contact email setup

Copy `server/.env.example` values into `server/.env`. Use a Gmail App Password for `GMAIL_APP_PASSWORD`; a normal Gmail password will not work. The contact form sends messages to `CONTACT_EMAIL` through Gmail SMTP, and the visitor's email is set as the reply-to address.
